version: '3.8'
services:
  gitlab:
    image: gitlab/gitlab-ce:latest
    container_name: gitlab-https
    hostname: gitlab.local
    restart: unless-stopped
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'https://gitlab.local:8443'
        gitlab_rails['gitlab_shell_ssh_port'] = 2223
        nginx['ssl_certificate'] = "/etc/gitlab/ssl/gitlab.crt"
        nginx['ssl_certificate_key'] = "/etc/gitlab/ssl/gitlab.key"
        nginx['redirect_http_to_https'] = true
        gitlab_rails['initial_root_password'] = 'securepass123!'
    ports:
      - "8081:80"      # HTTP редирект
      - "8443:443"     # HTTPS
      - "2223:22"      # SSH
    volumes:
      - gitlab_config_ssl:/etc/gitlab
      - gitlab_logs_ssl:/var/log/gitlab
      - gitlab_data_ssl:/var/opt/gitlab
      - ./ssl:/etc/gitlab/ssl
    networks:
      - gitlab-network-ssl

networks:
  gitlab-network-ssl:
    driver: bridge

volumes:
  gitlab_config_ssl:
  gitlab_logs_ssl:
  gitlab_data_ssl: